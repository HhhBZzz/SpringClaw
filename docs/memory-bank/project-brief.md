# Project Brief

SpringClaw 是 Java / Spring 生态的本地 Agent Runtime 与 Harness 雏形。

当前产品方向不是继续堆聊天 demo，而是沉淀可运行、可审计、可恢复、可控的 agent 基础设施。

核心目标：

- 让一次任务从请求、上下文、计划、工具、观察、确认、验证到结果都能被追踪。
- 让本地文件、命令、工具调用有明确边界和确认。
- 让项目记忆以可读、可版本化的方式沉淀，而不是只依赖向量召回。
